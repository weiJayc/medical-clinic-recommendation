# 團隊 Git 與 Pull Request 協作流程（新手版）
> Medical Clinic Recommendation 專案適用

本文件目的：  
讓 **不熟 Git 的組員** 也能照表操作、不亂推、不炸 repo。  
請所有組員在開始寫程式前 **務必閱讀一次**。

---

## 0️⃣ 核心原則（一定要遵守）

1. **禁止直接修改 `main` 分支**
   - `main`：穩定版本（Demo / 報告 / 交作業）
   - 所有開發都先進 `dev`

2. **每個人只在自己的 `feature/*` 分支工作**
3. **所有程式碼合併都必須透過 Pull Request（PR）**
4. **遇到問題先停下來**
   - 貼 `git status` 與錯誤訊息到群組
   - 不要自己亂試一堆指令

---

## 1️⃣ 分支規則（Branch Strategy）

| 分支 | 用途 |
|---|---|
| main | 穩定版、最終展示 |
| dev | 整合分支（大家的 PR 都合到這） |
| feature/姓名-功能 | 個人功能分支 |

範例：
- `feature/jay-map-ui`
- `feature/anna-nlp-mapping`
- `feature/kevin-api-nearby`

---

## 2️⃣ 組員第一次加入專案要做的事

### 2.1 安裝並設定 Git（只做一次）
```bash
git --version
git config --global user.name "你的名字"
git config --global user.email "你的email"
```

---

### 2.2 Clone 專案（只做一次）
```bash
git clone https://github.com/<owner>/<repo>.git
cd <repo>
```

---

### 2.3 切到 dev 並同步最新進度（每次開始前都要）
```bash
git checkout dev
git pull
```

---

## 3️⃣ 每次開始寫程式的標準流程（所有人都一樣，詳細見本文最下方appendix A)

### Step A：確認 dev 是最新的
```bash
git checkout dev
git pull
```

---

### Step B：從 dev 切換到自己的功能分支
如果是第一次創建分支
```bash
git checkout -b feature/<你的名字>-<任務>
```
如果已經創建了那就
```bash
git checkout <你的分支名稱>
```

---

### Step C：把自己的分支跟最新的dev同步
```bash
git checkout dev
git pull

git checkout <你的分支>
git rebase dev
```

---

### Step D：寫程式並 commit
```bash
git add .
git commit -m "feat: 說明你做了什麼"
```

Commit 訊息建議格式：
- `feat:` 新功能
- `fix:` 修 bug
- `docs:` 文件
- `chore:` 設定、整理

---

### Step E：推到 GitHub
如果branch是第一次創建，還沒有推送到Github過
```bash
git push -u origin feature/<你的名字>-<任務>
```
如果已經推送過了
```bash
git push
```

---

### Step E：在 GitHub 發起 Pull Request
- base：`dev`
- compare：你的 `feature/*` 分支

---

## 4️⃣ Pull Request（PR）流程說明

### 4.1 發起 PR 的人（Author）要做什麼

PR 描述請包含以下內容：

**① 做了什麼**
- 實作了哪個功能？

**② 怎麼測試**
- 如何確認這個功能正常？

**③ 影響範圍**
- 是否影響 API / DB / 其他模組？

**④ 備註**
- TODO、已知問題

#### Author 合併前自我檢查
- [ ] 沒有改 `main`
- [ ] 從最新 `dev` 開分支
- [ ] 只改到自己負責的資料夾
- [ ] 本地可以跑，不會直接報錯
- [ ] 沒有上傳 `.env`、API key、大型檔案

---

### 4.2 Review PR 的人（Reviewer）要做什麼

Reviewer 只需要確認三件事：

1. **改的東西合不合理**
2. **有沒有改到不該動的地方**
3. **功能是否符合 core_web_function.md**

#### Reviewer 檢查清單
- [ ] PR 是合到 `dev`
- [ ] 功能沒有超出專案範圍
- [ ] 沒有敏感資料或大型資料檔

---

### 4.3 如何合併 PR

- 原則上由隊長或指定整合者合併
- 建議使用 **Squash and Merge**
- 合併後，所有人請更新本地 `dev`：

```bash
git checkout dev
git pull
```

---

## 5️⃣ 發生衝突（Conflict）怎麼辦？

### PR 顯示有衝突時（Author 處理）

```bash
git checkout dev
git pull
git checkout feature/<你的分支>
git rebase dev
```

若出現衝突：
1. 用 VS Code 打開衝突檔案
2. 選擇正確版本（Accept Current / Incoming）
3. 解完後：
```bash
git add <檔案>
git rebase --continue
```

最後推上去：
```bash
git push --force-with-lease
```

⚠️ 不確定就貼畫面問隊長

---

## 7️⃣ 新手最常犯的錯誤（請避免）
- ❌ 直接 commit 到 `main`
- ❌ 沒 `git pull` 就開始寫
- ❌ 把 `.env`、金鑰推上去
- ❌ 一次 PR 改一大堆東西

---

## 8️⃣ 救命指令（出事先跑這些）
```bash
git status
git branch
git log --oneline --decorate -5
```

---

## 📎 附錄 A：關於 pull dev、rebase 與分支同步的補充說明（重要）

本附錄用來補充說明團隊在實際開發中，**最容易誤解的 Git 行為**。  
請所有組員至少閱讀一次，避免不必要的衝突與重工。

---

## A.1 為什麼「開始開發前」都要先 pull dev？

### 重點結論
> **pull dev 不會影響你的 feature 分支，但可以避免你在「過期的基準」上開發。**

實際原因包含：

1. **避免從舊的 dev 開分支**
   - 新的 feature 分支是「從當下的 dev 複製出來的」
   - dev 越舊，未來合併回 dev 時衝突風險越高

2. **避免重複做別人已完成的事**
   - 不 pull dev，看不到別人已 merge 的 API、UI、資料結構
   - 容易造成重工或命名衝突

3. **讓衝突提早且變小**
   - 每天同步 dev → 每次 rebase 差異小
   - 拖到最後才同步 → 一次爆大量衝突

---

## A.2 pull dev 會影響我的 feature 分支嗎？

### 答案：不會

執行以下指令時：
```bash
git checkout dev
git pull
```

只會發生：
- 本地 `dev` 更新 ✅
- 你的 `feature/*` 分支 **完全不變** ✅

Git 不會自動修改你不在的分支，這是刻意的安全設計。

---

## A.3 那是不是可以等到要開 Pull Request 時再 pull dev？

### 技術上：可以  
### 團隊實務上：**不建議**

適合「等到 PR 才同步 dev」的情況：
- 改動非常小
- 幾乎不會碰到共用檔案

不適合的情況（本專案大多屬於這類）：
- 多人同時改前端 UI / component
- 多人同時改後端 API / response schema
- DB / NLP / Backend 有資料結構依賴

**越晚同步 dev，最後一次 rebase 的成本越高。**

---

## A.4 隔天繼續開發，我還需要 pull dev 嗎？

### 情況 1：隔天繼續寫「同一支 feature 分支」（最常見）
建議流程：

```bash
git checkout dev
git pull

git checkout feature/你的分支
git rebase dev
```

原因：
- dev 通常每天都會有人 merge
- 不跟上只會讓差異越積越多

---

### 情況 2：隔天要開「新的 feature 分支」
一定要先 pull dev 再開分支：

```bash
git checkout dev
git pull
git checkout -b feature/new-task
```

---

## A.5 為什麼 pull dev 了，但我的 feature 還是沒更新？

因為這是 **Git 的正常行為**。

- pull dev：只更新 dev
- feature 要跟上 dev：**必須手動 rebase 或 merge**

推薦做法（本團隊標準）：
```bash
git checkout feature/你的分支
git rebase dev
```

---

## A.6 rebase 的角色定位（再次強調）

- rebase 是用來：
  - **整理「自己的工作」**
  - 把 feature 接到最新 dev 上
- rebase 不是用來：
  - 合併 feature → dev
  - 操作多人共用分支（dev / main）

### 團隊安全守則
- ✅ 可以 rebase：`feature/*`
- ❌ 不可以 rebase：`dev`、`main`

---

## A.7 本團隊建議的每日同步節奏（請照做）

### 每天第一次開始工作
```bash
git checkout dev
git pull
```

### 如果你有正在開發的 feature
```bash
git checkout feature/你的分支
git rebase dev
```

### 發 Pull Request 前（一定要）
```bash
git checkout dev
git pull
git checkout feature/你的分支
git rebase dev
```

---

### 一句話總結
> **pull dev 是同步團隊現況，rebase dev 是讓你的工作跟上現況。**  
> 兩件事缺一不可。

## 📎 附錄 B：Pull Request 合併後，branch 被刪除時的正確處理方式（重要）

本附錄說明以下情境：  
**當 Pull Request 已成功合併，且 GitHub 上的 feature branch 已被刪除後，本地端該如何同步與清理。**

這是團隊協作中非常常見的狀況，請依照以下流程處理。

---

## B.1 基本觀念（先看）

1. **刪除遠端 branch 不會影響已合併的程式碼**
   - PR merge 完成後，所有 commit 已經進入 `dev`
   - 刪除 branch 只是移除「指向該 commit 的分支指標」

2. **Git 不會自動刪除你本地的 branch**
   - 即使 GitHub 已刪除遠端 branch
   - 你的本地 branch 仍會存在，需手動清理

---

## B.2 標準處理流程（強烈建議照做）

假設被刪除的分支為：`feature/xxx`

### Step 1：切回 dev 並同步最新內容
```bash
git checkout dev
git pull
```

確保你本地的 `dev` 已包含該 PR 的合併結果。

---

### Step 2：同步遠端狀態並清理已不存在的遠端分支
```bash
git fetch --prune
```

這個指令會：
- 更新遠端分支清單
- 移除本地紀錄中「遠端已不存在」的 branch（如 `origin/feature/xxx`）

可用以下指令確認：
```bash
git branch -r
```

---

### Step 3：刪除本地已完成的 feature branch（建議）
```bash
git branch -d feature/xxx
```

- `-d` 為**安全刪除**
- Git 會確認該 branch 已被合併，才允許刪除

若 Git 提示無法刪除，但你**確定不需要該 branch**，可使用：
```bash
git branch -D feature/xxx
```

⚠️ `-D` 為強制刪除，請確定該 branch 不再需要。

---

## B.3 常見情境與對應處理

### 情境 1：本地 branch 還有未 commit 的變更
```bash
git status
```

#### 解法 A：先 commit（最安全）
```bash
git add .
git commit -m "wip: save local changes"
```

#### 解法 B：暫存變更（不想 commit）
```bash
git stash -u
git checkout dev
```

需要時再取回：
```bash
git stash pop
```

---

### 情境 2：本地 branch 還有未合併的 commit（但 PR 已關閉）
建議先保留備份：

```bash
git checkout feature/xxx
git checkout -b feature/xxx-backup
```

再回到 `dev` 進行清理。

---

## B.4 團隊建議的一條龍指令（最實用）

PR 合併並刪除 branch 後，所有人可直接執行：

```bash
git checkout dev
git pull
git fetch --prune
git branch -d feature/xxx
```

---

## B.5 為什麼一定要清理 branch？

- 避免本地殘留大量已完成分支，造成混亂
- 確保下次開發從乾淨狀態開始
- 避免誤在舊 branch 上繼續開發

---

### 一句話總結
> **PR merge 完、branch 刪掉後，本地要：同步 dev → prune → 刪 branch。**

---
